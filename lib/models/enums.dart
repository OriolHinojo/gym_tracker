enum Equipment { barbell, dumbbell, machine, cable, bodyweight, kettlebell, band }

enum ProgressionRule { rir, rpe, simple }

enum FeltFlag { hard, good, easy }

enum PRType { oneRm, fiveRm, e1rmBest, weightAtRep }


